apiVersion: apps/v1
kind: Deployment
metadata:
  name: mlflow-deployment
  namespace: skill-ex-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mlflow
  template:
    metadata:
      labels:
        app: mlflow
    spec:
      containers:
        - name: mlflow
          image: aie007/microservices-mlflow:latest
          ports:
            - containerPort: 5000